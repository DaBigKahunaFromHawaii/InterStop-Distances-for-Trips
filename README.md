# InterStop-Distances-for-Trips

This script calculates the distances between each stop in sequence given by trips. This also calculates the total scheduled time for each trip by summing the time alloted per stop and the layover/recovery time for each trip.
